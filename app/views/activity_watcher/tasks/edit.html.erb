<%= simple_form_for @task, url: _course_task_path, wrapper: "horizontal_form" do |f| %>
  <%= render 'form', f: f, task: @task %>
<% end %>

<%= link_to '戻る', _course_tasks_path(@task.course_id), class: "btn btn-default btn-back" %>
